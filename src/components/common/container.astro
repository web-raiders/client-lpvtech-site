---
const { flex = "flex", justify = "justify-between" } = Astro.props;

interface Props {
  flex?: string;
  justify?: string
}
---
<div class=`w-[90%] mx-auto ${flex} ${justify} items-center md:w-4/5`>
  <slot />
</div>